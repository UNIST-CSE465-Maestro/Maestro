<?xml version="1.0" encoding="utf-8"?>
<!--
  HTTPS is required by default. Cleartext HTTP is permitted only for the
  Question Encoder (QE) server, which is served over plain HTTP on a custom
  port (http://h-router.iptime.org:9511) and for local development hosts.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">h-router.iptime.org</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
